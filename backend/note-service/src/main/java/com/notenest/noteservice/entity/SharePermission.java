package com.notenest.noteservice.entity;

// Sengaja cuma READ untuk sekarang. Enum (bukan boolean) supaya menambah
// EDIT/ADMIN nanti tidak perlu mengubah bentuk tabel.
public enum SharePermission {
    READ
}
